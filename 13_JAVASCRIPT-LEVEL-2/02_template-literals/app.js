// Template Literals: they are used to embed expressions in the strings.

let pencilPrice = 10;
let eraserPrice = 5;
// let output = "The total sum is: " + (pencilPrice + eraserPrice) + " rupees";
// let output = `The total sum is: ${pencilPrice + eraserPrice} rupees`;
// console.log(output);

console.log(`The total sum is: ${(pencilPrice + eraserPrice)} rupees`);